package tests;

public class TestAnalysis {
    private TestAnalysis () { //konstruktor prywatny aby nie utworzyć instancji tej klasy,
        TestAnalysis t = new TestAnalysis();   //obiekt = new  - instancja obiektu
    }
}
